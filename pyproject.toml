[project]
name = "mcroons-seedance-webhook"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.115.6",
    "requests==2.31.0",
    "python-dotenv==1.0.0",
]

[tool.vercel]
entrypoint = "api.seedance:app"
